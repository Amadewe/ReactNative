import {StyleSheet} from 'react-native';
import colors from './variables.js';
const billetdetailStyles = StyleSheet.create({
    BilletDetail: {
        flex: 1,
    }
    });
export default billetdetailStyles